name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: yomitan-dict-builder

    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: yomitan-dict-builder

      - name: Check code format
        run: cargo fmt --all --check

      - name: Test static AniList index builder
        run: python scripts/test_refresh_anilist_media_index.py

      - name: Test media autocomplete helpers
        run: node scripts/test_anilist_media_autocomplete.js

      - name: Run tests
        run: cargo test
